export const metadata = {
  title: "Settings",
  description: "Settings page, update username and image",
};

export default function PostLayout({ children }) {
  return <section>{children}</section>;
}
